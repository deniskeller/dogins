import { Default } from '@layouts/index';
import { Compliance } from '@view/landing';

const CompliancePage = () => {
	return (
		<Default>
			<Compliance />
		</Default>
	);
};

export default CompliancePage;
