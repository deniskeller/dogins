import { Default } from '@layouts/index';
import { Contacts } from '@view/landing';

const ContactsPage = () => {
	return (
		<Default>
			<Contacts />
		</Default>
	);
};

export default ContactsPage;
