import { Empty } from '@layouts/index';
import { ComingSoon } from '@view/landing';

const ComingSoonPage = () => {
	return (
		<Empty>
			<ComingSoon />
		</Empty>
	);
};

export default ComingSoonPage;
