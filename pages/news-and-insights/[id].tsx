import { Default } from '@layouts/index';
import { SelectedNews } from '@view/landing';

const NewsPage = () => {
	return (
		<Default>
			<SelectedNews />
		</Default>
	);
};

export default NewsPage;
